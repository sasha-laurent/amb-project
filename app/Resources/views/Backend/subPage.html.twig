{% extends app.request.isXmlHttpRequest ? '::minimum.html.twig' : "::layout.html.twig" %}

{% block title %}
  {{ mainTitle }} - {{ parent() }}
{% endblock %}

{% block h1_title %}
	<div class="row">
		<div class="col-md-10">
			{% if backButtonUrl is defined %}
				<a class="btn btn-default" href="{{ backButtonUrl }}" >
					<span class="left glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
				</a>
			{% endif %}
			{{ mainTitle }}
		</div>
		<div class="col-md-2 text-right">
			<div class="btn-group" role="group" aria-label="...">
				{% if saveButton is defined %}
					<a class="btn btn-default" id="save-btn">
						<span class="left glyphicon glyphicon-floppy-disk" aria-hidden="true"></span>
					</a>
				{% endif %}
				{% if addButtonUrl is defined %}
					<a class="btn btn-default" {% if addModal is defined %} data-toggle="modal" data-target="{{ addModal }}" {% else %} href="{{ addButtonUrl }}" {% endif %} >
						<span class="left glyphicon glyphicon-plus" aria-hidden="true"></span>
					</a>
				{% endif %}
				{% if editButtonUrl is defined %}
					<a class="btn btn-default" {% if editModal is defined %} data-toggle="modal" data-target="{{ editModal }}" {% else %} href="{{ editButtonUrl }}" {% endif %} >
						<span class="left glyphicon glyphicon-pencil" aria-hidden="true"></span>
					</a>
				{% endif %}
				{% if delButtonUrl is defined %}
					<a class="btn btn-default" href="{{ delButtonUrl }}" >
						<span class="left glyphicon glyphicon-remove" aria-hidden="true"></span>
					</a>
				{% endif %}
			</div>
		</div>
	</div>	
{% endblock %}
